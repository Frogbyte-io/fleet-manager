machine:
  id: test-ubuntu
  host: proxmox-01
  os: ubuntu-desktop-24.04
  roles:
    - desktop-test

lifecycle:
  mode: ephemeral
  reset_strategy: clone
  base_template: ubuntu-desktop-24.04-v2

requires:
  devices:
    - decker-controller
